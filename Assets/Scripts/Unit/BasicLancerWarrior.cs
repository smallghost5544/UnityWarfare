using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BasicLancerWarrior : MonoBehaviour, IExportable
{
    public void Export()
    {
        //進戰單位可以巡邏或蓋防禦工事
        Debug.Log("doingWell");
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
